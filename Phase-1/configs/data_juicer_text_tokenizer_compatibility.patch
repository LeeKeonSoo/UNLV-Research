diff --git a/data_juicer/utils/model_utils.py b/data_juicer/utils/model_utils.py
--- a/data_juicer/utils/model_utils.py
+++ b/data_juicer/utils/model_utils.py
@@ -699,8 +699,13 @@ def prepare_huggingface_model(
                 model_params["device"] = device
                 logger.warning("accelerate not found, using device directly")
 
     pretrained_model_name_or_path = check_model_home(pretrained_model_name_or_path)
-    processor = transformers.AutoProcessor.from_pretrained(pretrained_model_name_or_path, **model_params)
+    try:
+        processor = transformers.AutoProcessor.from_pretrained(pretrained_model_name_or_path, **model_params)
+    except ValueError:
+        # Text-only checkpoints such as Pythia expose a tokenizer but no
+        # processor class. Preserve the operator metric with that tokenizer.
+        processor = transformers.AutoTokenizer.from_pretrained(pretrained_model_name_or_path, **model_params)
 
     if return_model:
         config = transformers.AutoConfig.from_pretrained(pretrained_model_name_or_path, **model_params)
