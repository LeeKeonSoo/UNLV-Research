# Core ML libraries
# CUDA wheels for Windows/Linux are served from the PyTorch index below.
--extra-index-url https://download.pytorch.org/whl/cu121
transformers==5.0.0

# PyTorch (platform-specific)
torch==2.5.1+cu121; platform_system == "Windows" or platform_system == "Linux"
# macOS: use non-CUDA builds; torch 2.5.1 wheels are unavailable on newer Python.
torch>=2.6.0; platform_system == "Darwin"

# torchvision/torchaudio are intentionally omitted (not used in this project).
sentence-transformers>=2.2.0

# Data processing
datasets==3.2.0
numpy==2.2.1
pandas>=2.0.0

# Text analysis
textstat>=0.7.3
nltk>=3.8.0

# Visualization
plotly==6.5.2
matplotlib>=3.7.0
seaborn>=0.12.0

# Deduplication
datasketch>=1.5.9

# Utilities
tqdm
jsonlines>=3.1.0
scikit-learn
